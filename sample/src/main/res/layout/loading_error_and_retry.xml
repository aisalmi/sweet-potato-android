<?xml version="1.0" encoding="utf-8"?>
<layout
  xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:app="http://schemas.android.com/apk/res-auto"
  >
  <data>
    <variable
      name="stateManager"
      type="com.hagergroup.sweetpotato.lifecycle.SweetViewModel.StateManager"
      />
  </data>

  <RelativeLayout
    android:id="@+id/loadingErrorAndRetry"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:clickable="true"
    android:focusable="true"
    android:gravity="center"
    app:animatedVisibility="@{stateManager.errorAndLoadingViewVisibility}"
    >
    <LinearLayout
      android:id="@+id/errorAndRetry"
      android:layout_width="match_parent"
      android:layout_height="match_parent"
      android:background="@color/background"
      android:gravity="center"
      android:orientation="vertical"
      android:paddingStart="10dp"
      android:paddingEnd="10dp"
      >
      <TextView
        android:id="@+id/errorText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="40dp"
        android:gravity="center"
        android:text="@{stateManager.errorMessage}"
        android:textSize="18sp"
        />

      <Button
        android:id="@+id/retry"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:text="@string/retry"
        />
    </LinearLayout>

    <include
      layout="@layout/loading"
      app:stateManager="@{stateManager}"
      />
  </RelativeLayout>
</layout>